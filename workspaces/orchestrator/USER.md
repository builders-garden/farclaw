# User Context

- Project: ${PROJECT_NAME}
- Farcaster handle: ${FARCASTER_HANDLE}
- Tone source FID: ${FARCASTER_FID}
- Default channel id (slug): ${FARCASTER_CHANNEL_ID}

Notes:
- Empty values are intentional until configured.
- Use this file as runtime context, not as truth for factual claims.
